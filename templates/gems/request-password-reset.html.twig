{% extends '@gems/layout.html.twig' %}

{% block content %}
    <form method="post">
        <div class="form-group">
            <label class="control-label" for="organization">{{ trans('Organization') }}</label>
            <select class="form-control" name="organization" id="organization">
                {% for id, name in organizations %}
                    <option value="{{ id }}" {{ (id == (input.organization ?? null)) ? 'selected' }}>{{ name }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="form-group">
            <label class="control-label" for="username">{{ trans('Username') }}</label>
            <input type="text" class="form-control" name="username" id="username" value="{{ input.username ?? '' }}">
        </div>

        <button type="submit" class="btn btn-primary">{{ trans('Request reset') }}</button>
    </form>

    <a href="{{ route('auth.login') }}">{{ trans('Back to login') }}</a>
{% endblock %}
