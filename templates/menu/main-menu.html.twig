{% if children is not empty %}
    <ul class="menu-children navigation nav nav-stacked flex-column">
        {% for child in children %}
            {{ child.renderNode()|raw }}
        {% endfor %}
    </ul>
{% endif %}
