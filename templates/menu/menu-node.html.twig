{{ renderedItem|raw }}

{% if children is not empty %}
    <ul class="menu-children navigation nav nav-stacked" style="margin-left: 20px;">
        {% for child in children %}
            {{ child.renderNode()|raw }}
        {% endfor %}
    </ul>
{% endif %}
